"""
AIA V2.00.1 — Workers
======================
Delta Phase Warthog

All workers fire simultaneously into WHITE (+1) superposition.
Queen's Fold collapses through GRAY (0) into BLACK (-1) sealed memory.

Worker color plane assignments:
  curiosity_worker  → orange  → RGB(255,140,0)  → 520hz
  emotion_worker    → red     → RGB(255,0,0)    → 700hz
  language_worker   → blue    → RGB(0,0,255)    → 450hz
  logic_worker      → blue    → RGB(0,0,255)    → 450hz
  memory_worker     → violet  → RGB(148,0,211)  → 420hz
"""
from core.q_constants import BLACK, GRAY, WHITE
